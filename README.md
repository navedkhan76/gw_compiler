# GW_compiler
This is a compiler for GW Basic build using lex and yacc
This follows the language rules as listed on https://hwiegman.home.xs4all.nl/gw-man/index.html.
It scans the program, spits the lexical tokens and then processed into syntax tree, which in turn used for producing the machine code
